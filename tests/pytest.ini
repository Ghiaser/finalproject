[pytest]
# 1. מצרף את הספרייה הנוכחית ל־PYTHONPATH כדי שייבוא מודולים כמו 'app.main' יעבוד
pythonpath = .

# 2. אפשר להגדיר סימונים (markers) מותאמים אישית, אם יש צורך:
# markers =
#     integration: סימון לבדיקות אינטגרציה
#     slow: סימון לבדיקות איטיות

# 3. קבצים פופולריים ש־pytest מתעלם מהם בעת סריקה (תוסיפו אם ברצונכם):
# norecursedirs = .git .venv venv __pycache__

# 4. אפשר לשלוט גם על רמת ה־verbosity (לא חובה במידה ומשתמשים ב־-q או בלינוקס/CLI)
# addopts = -q --disable-warnings

